# Work Log

## 2026-03-10

- 何をしたか
  - GNOME Shell 拡張の最小構成として `metadata.json`、`extension.js`、`snippets.txt` を追加した。
  - 固定ファイル `snippets.txt` を `%%snippet: タイトル%%` 記法で分割して、トップバーのメニューに出す実装を入れた。
  - 候補選択時にクリップボードへコピーし、その後に `Ctrl+V` の送出を試みる処理を追加した。
  - `.codex/work-log.md` を作成し、作業内容と判断理由を記録した。
  - `gnome-extensions` で有効化を試したが、現セッションでは新規拡張がまだ認識されていないことを確認した。
- なぜそうしたか
  - 空行や Markdown 記法を本文で自由に使えるようにするため、専用の開始マーカー方式にした。
  - Wayland/X11 の差異があるため、最低限の成功ラインを「コピー成功」に置きつつ、自動貼り付けは best-effort にした。
  - Wayland セッションでは新規拡張の認識にログアウト・ログインが必要になることがあるため、現時点の状態を記録した。
- 未完了の事項
  - 実機でのロード確認と、Wayland/X11 ごとの貼り付け成功率の確認。
  - アイコンや設定画面、検索 UI などの改善。
- 次にやるとよいこと
  - `gnome-extensions enable snippet-picker@jidaikobo.shibata` で有効化して挙動確認する。
  - その前に Wayland セッションをログアウト・ログインして、拡張一覧に現れることを確認する。
  - 必要なら固定ファイルパスを設定画面へ切り出す。
